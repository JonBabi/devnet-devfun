# Credentials for Extreme Networks Switch Collector
# This file uses simple KEY=VALUE format - no escaping needed!
# Copy this file to 'credentials.env' and fill in your credentials.
#
# Unlike JSON, you can use special characters like \ and / without escaping.

SWITCH_USERNAME=admin
SWITCH_PASSWORD=your_password_here

# Optional: Enable password (defaults to SWITCH_PASSWORD if not specified)
# SWITCH_ENABLE_PASSWORD=your_enable_password_here
